{
    "scripts": {
        "start-electron": "cd electron && npm start",
        "start-react": "cd frontend && npm start --open false",
        "build-react": "cd frontend && npm run build",
        "start-rails": "cd backend && rails server",
        "init-rails": "cd backend && bundle install && rails db:create && rails db:migrate",
        "start": "npm run dev",
        "dev": "concurrently \"npm run start-rails\" \"npm run start-react\"",
        "electron-build": "concurrently \"npm run start-rails\" \"npm run build-react\" \"BROWSER=none npm run start-react\" \"npm run start-electron\"",
        "init-openmarch": "npm install && cd electron && npm install && cd ../frontend && npm install && cd.. && npm run init-rails"
    },
    "devDependencies": {
        "concurrently": "^8.2.2",
        "wait-on": "^7.2.0"
    }
}
