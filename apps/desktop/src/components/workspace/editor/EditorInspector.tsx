import Inspector from "@/components/inspector/Inspector";

/**
 * Drill editor inspector — composition root for editor workspace mode.
 */
export default function EditorInspector() {
    return <Inspector />;
}
