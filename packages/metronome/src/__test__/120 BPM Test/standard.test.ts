import { describe, expect, test } from "vitest";

import { type Measure, type Beat } from "../../metronome";

const testName = "Standard 4/4 120 BPM Test";

/**
 * Empty Dummy test case.
 */
describe(testName, () => {
    test(`Synthesize ${testName}`, async () => {
        // Setup
        // Create audio file from Measures
        // Write the audio file
    });
});

/**
 * Replace the expected output with the actual expected output for your test case.
 */
const expected: Measure[] = [];
