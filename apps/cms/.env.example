# A generated secret for Payload - run `echo PAYLOAD_SECRET=\"$(openssl rand -base64 64)\" > .env` to generate a new one
PAYLOAD_SECRET=

# Secret for blog preview URLs (website /blog/preview?id=...&token=...). Omit to disable preview endpoint.
PREVIEW_SECRET=

# Optional: site URL for "Preview on site" link in Posts edit view (default: https://openmarch.com).
# WEBSITE_URL=

# Optional: Cloudflare Pages deploy hook URL. When set, changing a post's status triggers a website rebuild.
# Create the hook in Pages: Settings > Builds > Add deploy hook. Omit to disable.
# WEBSITE_DEPLOY_HOOK_URL=
