// This is a file that contains strings of pre-defined bootstrap classes.
// This is done to avoid retyping the same class names over and over again.

export const bsconfig = {
    tableHeader: "row p-1 mx-auto bg-secondary text-light",
    table: "table table-sm table-hover"
}
